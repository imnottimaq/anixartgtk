using Gtk 4.0;
using Adw 1;

Adw.NavigationPage dub-selection {
  child: Adw.ToolbarView {
    [top]
    Adw.HeaderBar {}
    content: Gtk.ScrolledWindow {
      Gtk.Box dub-main {
        orientation: vertical;
        spacing: 12;
      }
    };
  };
}
